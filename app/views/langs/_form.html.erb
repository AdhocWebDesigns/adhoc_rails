<%= form_with(model: lang, local: true) do |form| %>
  <% if lang.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lang.errors.count, "error") %> prohibited this lang from being saved:</h2>

      <ul>
      <% lang.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :desc %>
    <%= form.text_area :desc %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
