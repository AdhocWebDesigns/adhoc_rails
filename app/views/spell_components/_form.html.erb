<%= form_with(model: spell_component, local: true) do |form| %>
  <% if spell_component.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(spell_component.errors.count, "error") %> prohibited this spell_component from being saved:</h2>

      <ul>
      <% spell_component.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :spell_component_type_id %>
    <%= form.text_field :spell_component_type_id %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :desc %>
    <%= form.text_area :desc %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
